<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="materialButtonStyle">@style/ModernButton</item>
        <item name="textInputStyle">@style/ModernInput</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/ai_accent</item>
        <item name="colorSecondaryContainer">@color/glass_chip</item>
        <item name="colorOnSecondaryContainer">@color/colorPrimary</item>
        <item name="colorPrimaryContainer">@color/glass_chip</item>
        <item name="colorOnPrimaryContainer">@color/colorPrimary</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="colorOnSurfaceVariant">@color/text_secondary</item>
        <item name="colorSurfaceVariant">@color/glass_panel_soft</item>
        <item name="colorOutline">@color/glass_stroke_strong</item>
        <item name="android:colorBackground">@color/background_light</item>
        <item name="android:windowBackground">@color/background_light</item>
        <item name="android:navigationBarColor">@color/surface</item>
        <item name="colorContainer">@color/background_light</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Material 3 Specifics -->
        <item name="textAppearanceTitleLarge">@style/ModernTitleLarge</item>
    </style>

    <style name="ModernTitleLarge" parent="TextAppearance.Material3.TitleLarge">
        <item name="android:textStyle">bold</item>
        <item name="android:letterSpacing">0</item>
    </style>

    <style name="ModernCardStyle" parent="Widget.Material3.CardView.Elevated">
        <item name="cardCornerRadius">24dp</item>
        <item name="cardElevation">2dp</item>
    </style>

    <!-- CORRECTED: Add the missing overlay theme definitions. -->
    <style name="Theme.MyApplication.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.MyApplication.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
